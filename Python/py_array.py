import numpy as np

array = [2,3,1]
print(array[1:]) # print[3,1] it means that the print is from the 1 to the end
print(array[:-1]) # it mean that the print is from the 0 to the element before -1 so the index is a range like [) , 
                  #the left is close and the righ is open
                